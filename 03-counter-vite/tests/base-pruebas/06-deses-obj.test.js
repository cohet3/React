import { usContext } from "../../src/base-pruebas/06-deses-obj";

describe('Tarea 06 igual que la 05', () => {
    test('usContext tiene que rotornar un objeto', () =>{
        const usContext = ({}) => {
            expect( user ).toStrictEqual({
                const nombre = 'rango',
                const anios: 'edad',
                const username: 'nombreClave',
                const latlng: {
                    lat: 14.1232,
                    lng: -12.3232
                }
            });
    }
})




